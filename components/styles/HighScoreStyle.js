import { StyleSheet } from 'react-native';

export default StyleSheet.create({

    backButton: {
        marginTop: 60,
        marginBottom: 15,
        width: '80%',
        alignSelf: 'center',
        justifyContent: 'center',
        borderColor: 'crimson'
    }
});